'use strict';

var gulp-postcss-cached = require('../index');

describe('gulp-postcss-cached', function() {
  it('works', function() {
    expect(gulp-postcss-cached()).toBe('Hello World!');
  });
});
